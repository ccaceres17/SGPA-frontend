# Server-only. Base URL of the SGPA backend API.
# Falls back to the current production API if unset — see src/lib/components/Tokens.js.
# Never prefix this with PUBLIC_: it must not be exposed to client-side code.
API_BASE_URL=https://academic-project-management-api.onrender.com/api
